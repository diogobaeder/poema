---

- name: Installing pip
  easy_install:
    name: pip
    state: latest
    executable: ~/bin/easy_install-{{ server.python_version }}

- name: Installing virtualenv
  pip:
    <<: &pip_base
      state: latest
      executable: ~/bin/pip{{ server.python_version }}
    name: virtualenv

- name: Installing virtualenvwrapper
  pip:
    <<: *pip_base
    name: virtualenvwrapper
